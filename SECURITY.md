# 安全策略

## 报告漏洞

如发现安全漏洞，请勿公开提交 Issue。通过以下方式私密报告：

1. **GitHub Security Advisory**（推荐）：进入仓库 → Security → Report a vulnerability
2. **邮件**：联系仓库维护者

请在报告中说明：

- 漏洞描述与影响范围
- 复现步骤（最小示例）
- 受影响的版本
- 建议的修复方案（如有）

我们将在 **48 小时内**确认收到报告，并在确认后 **7 天内**发布修复版本。

## 密钥安全

- **严禁在代码或提交历史中硬编码 API key、密码、令牌等敏感信息**（开发规范 §8）
- 所有密钥经环境变量读取，本地开发使用 `.env` 文件（已被 `.gitignore` 排除）
- CI 已接入 `gitleaks` 全历史扫描，发布前必须确认扫描通过

## 历史密钥处理

已知历史提交中曾包含硬编码的 DeepSeek API Key，该密钥已在 DeepSeek 平台吊销。如发现历史中仍有敏感信息泄露，请按上述流程报告。

## 支持的版本

| 版本 | 安全更新支持 |
|---|---|
| 0.2.x | ✅ 当前版本 |
| < 0.2.0 | ❌ 不再维护 |

## 安全相关配置

- `.gitignore` 已排除 `.env` / `.chroma/` / `*.sqlite3` 等敏感文件
- `OpenAIProvider` 默认关闭 SDK 内置重试（`max_retries=0`），由框架统一管理
- 工具执行失败在 ReAct 循环内以文本写回模型处理，不直接中断（可恢复信号）
